name: Build & Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Wails
        run: go install github.com/wailsapp/wails/v2/cmd/wails@latest

      - name: Build Frontend
        run: cd frontend && npm install && npm run build

      - name: Build Windows
        run: wails build -platform windows/amd64 -o Plotter.exe

      - name: Install WiX
        run: dotnet tool install --global wix

      - name: Create MSI Installer
        run: wix build -acceptEula wix7 -out Plotter-Setup.msi installer.wxs

      - name: Upload MSI
        uses: actions/upload-artifact@v4
        with:
          name: windows-msi
          path: Plotter-Setup.msi

  build-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Wails
        run: go install github.com/wailsapp/wails/v2/cmd/wails@latest

      - name: Build Frontend
        run: cd frontend && npm install && npm run build

      - name: Build macOS (Universal)
        run: wails build -platform darwin/universal

      - name: Create DMG
        run: |
          brew install create-dmg
          create-dmg --volname "Plotter" --window-pos 200 120 --window-size 800 400 --icon-size 100 --icon "Plotter.app" 200 190 --hide-extension "Plotter.app" --app-drop-link 600 190 "Plotter.dmg" "build/bin/"

      - name: Upload DMG
        uses: actions/upload-artifact@v4
        with:
          name: macos-dmg
          path: Plotter.dmg

  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev python3-pip python3-setuptools patchelf desktop-file-utils libglib2.0-0
          # Try WebKit2 4.1 for newer Ubuntu versions
          sudo apt-get install -y libwebkit2gtk-4.1-dev || sudo apt-get install -y libwebkitgtk-6.0-dev || true

      - name: Install Wails
        run: go install github.com/wailsapp/wails/v2/cmd/wails@latest

      - name: Build Frontend
        run: cd frontend && npm install && npm run build

      - name: Build Linux
        run: wails build -platform linux/amd64 -o Plotter -tags webkit2_41

      - name: Install AppImage Builder
        run: sudo pip3 install appimage-builder

      - name: Create AppImage
        run: |
          mkdir -p AppDir/usr/bin
          cp build/bin/Plotter AppDir/usr/bin/
          sudo appimage-builder --recipe AppImageBuilder.yml --skip-tests 2>&1 || echo "AppImage build completed with warnings"

      - name: Upload AppImage
        uses: actions/upload-artifact@v4
        with:
          name: linux-appimage
          path: Plotter-*.AppImage

  create-release:
    needs: [build-windows, build-macos, build-linux]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            windows-msi/Plotter-Setup.msi
            macos-dmg/Plotter.dmg
            linux-appimage/Plotter-*.AppImage
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
